var guestList = ["Angela", "Joel", "Jack", "Mods", "Soren"];

var person = "Soren";

if (guestList.includes(person)) {
  console.log(person + " Is on the list!");
} else {
  console.log(person + " Is not on the list!");
}
